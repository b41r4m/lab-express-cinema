<h1>Click on the movies to check the showtimes!</h1>

<div class="movies-list">
  {{#each movies}}
    <div class="movie-card">
      <img src={{image}} alt={{title}} />
      <div>{{this.title}}</div>
      <a href="/movies/{{_id}}">See more</a>
    </div>
  {{/each}}
</div>